import React from 'react'
import ScanPage from './pages/scan'
import './App.css'

interface IProps { }

const App: React.FC<IProps> = (props: IProps) => {
  return (
    <ScanPage />
  )
}
export default App
